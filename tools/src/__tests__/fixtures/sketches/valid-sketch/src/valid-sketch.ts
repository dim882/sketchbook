// valid-sketch - A complete sketch for testing
import { someUtil } from './valid-sketch.utils';

export const sketchName = 'valid-sketch';

export function main() {
  console.log(`Running ${sketchName}`);
  someUtil();
}
